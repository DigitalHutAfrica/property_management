<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_account_payment_form_inherit" model="ir.ui.view">
            <field name="name">account.payment.form.inherit.view</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_form"/>
            <field name="arch" type="xml">
                <field name="partner_bank_id" position="after">
                    <field name="property_id"/>
                    <field name="utility_bill_id" readonly="1" invisible="1"/>
                </field>
            </field>
        </record>


        <record id="view_utility_bill_register_form" model="ir.ui.view">
            <field name="name">utility.bill.register.form</field>
            <field name="model">utility.bill.register</field>
            <field name="arch" type="xml">
                <form string="Register Payment">
                    <group>
                        <group name="group1">
                            <field name="journal_id" options="{'no_open': True, 'no_create': True}" required="1"/>
                            <field name="payment_method_line_id"
                                   required="1" options="{'no_create': True, 'no_open': True}"/>
                            <field name="show_partner_bank_account" invisible="1"/>
                            <field name="is_multiple" invisible="1"/>
                            <field name="partner_bank_id"
                                   context="{'default_allow_out_payment': True}"
                                   invisible="is_multiple"/>

                        </group>
                        <group name="group2">
                            <label for="amount" invisible="is_multiple"/>
                            <div name="amount_div" class="o_row" invisible="is_multiple">
                                <field name="amount" invisible="is_multiple"/>
                                <field name="currency_id"
                                       required="1"
                                       invisible="is_multiple"
                                       options="{'no_create': True, 'no_open': True}"
                                       groups="base.group_multi_currency"/>
                            </div>
                            <field name="payment_date"/>
                            <field name="communication" invisible="is_multiple"/>
                        </group>

                    </group>
                    <footer>
                        <button string="Create Payment" name="action_create_payments" type="object" class="oe_highlight"
                                data-hotkey="q"/>
                        <button string="Cancel" class="btn btn-secondary" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>
